// Re-export types from voice-provider package for backward compatibility
export type {
  VoiceProviderType as VoiceProvider,
  VoiceSettings,
  VoiceStreamState,
  IVoiceProvider as VoiceProviderInterface
} from '@voice-provider/core';
